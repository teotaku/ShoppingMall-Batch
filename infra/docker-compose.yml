services:
  postgresql:
    image:
      postgres:latest
    env_file:
      - .env
    restart: always
    volumes:
      - ./data/shoppingmall-db:/var/lib/postgresql/data


    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
